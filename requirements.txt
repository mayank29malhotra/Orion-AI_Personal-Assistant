## Core Dependencies (loose, modern ranges)
gradio>=4.10.0
langgraph>=0.2.0
python-dotenv>=1.0.0
langchain>=0.0.300
langchain-core>=0.2.0
langchain-openai>=0.1.0
langchain-google-genai>=1.0.0
langchain-community>=0.2.0
langchain-experimental>=0.0.60
langchain-groq>=0.1.0
pydantic>=2.2.0
typing-extensions>=4.5.0

## Browser Automation
playwright>=1.40.0

## HTTP & Networking
httpx>=0.24.0
requests>=2.31.0
urllib3>=2.0.0

## Email & Calendar (Google APIs)
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

## Document Processing
pypdf>=3.0.0
pdf2image>=1.16.3
reportlab>=4.0.0
openpyxl>=3.1.0
xlrd>=2.0.1

## Data Processing
pandas>=2.0.0
numpy>=1.24.0

## Image Processing & OCR
Pillow>=10.0.0
pytesseract>=0.3.10
qrcode>=7.4.2

## Text Processing
markdown>=3.5.0
html2text>=2020.1.16

## Wikipedia / Search
wikipedia>=1.4.0
google-search-results>=2.4.2

## Utilities
pyperclip>=1.8.2

## MULTI-CHANNEL INTEGRATIONS
fastapi>=0.109.0
uvicorn>=0.27.0

## YouTube Tools
youtube-transcript-api>=0.6.0
yt-dlp>=2024.1.0
youtube-search-python>=1.6.6

## Optional (Windows service support)
# pywin32>=306

## Optional Redis backend (Phase 6.0+)
## When REDIS_ENABLED=true and REDIS_URL is reachable, Orion uses Redis for:
##   - Distributed per-user rate limiting (Phase 6.1)
##   - LangGraph checkpoint persistence across restarts/instances (Phase 6.2)
## Falls back gracefully to in-memory implementations when Redis is absent.
redis>=5.0.0

## Phase 6.2: RedisSaver checkpointer for LangGraph (drop-in BaseCheckpointSaver).
## Requires a Redis Stack server (RediSearch module) at runtime when enabled.
## Falls back to MemorySaver when Redis is unavailable or instantiation fails.
langgraph-checkpoint-redis>=0.4.0

## Test-only: in-process Redis fake used by tests/test_phase6_*.py.
## Safe to leave installed in production (zero runtime cost).
fakeredis>=2.20.0

## Phase 8: Swiggy MCP integration (Food, Instamart, Dineout)
## Optional. When SWIGGY_ENABLED=true + SWIGGY_ACCESS_TOKEN set, Orion connects
## to Swiggy Builders Club MCP servers over streamable HTTP and exposes 35
## food/grocery/dineout tools to the LLM. Falls back to disabled gracefully.
langchain-mcp-adapters>=0.1.0

